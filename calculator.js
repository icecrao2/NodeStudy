

const defaultnum = 1;

const add = (a, b) => a + b;
const minus = (a, b) => a - b;
const mul = (a, b) => a * b;
const divide = (a, b) => a/b;

module.exports = {
  defaultnum,
  add,
  minus,
  mul,
  divide
}